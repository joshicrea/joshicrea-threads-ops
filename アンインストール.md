# アンインストール

本体・データ・プラグイン登録がすべて消えます。データ（投稿・コメント・キー・トークン）も消えるので、残したいものがあれば先に `~/.claude/threads-ops/data/` をコピーしておいてください。

## Windows

1. `~/.claude/threads-ops/app/アンインストール.cmd` をダブルクリック。「続けますか？」に `y` と答えると、自動起動の解除 → 本体停止 → プラグイン登録の削除 → フォルダ削除まで自動で進みます
2. Claude Code を閉じて開き直す
3. Threads 側の連携を切りたいときは、Threads アプリの 設定 → アカウント → ウェブサイトのアクセス許可 から解除

`~` は `C:\Users\<名前>` です。アンインストール.cmd が見つからない（古い版で入れた）ときは、`自動起動を解除.cmd` → `停止.cmd` → フォルダ `~/.claude/threads-ops/` を削除 → Claude Code のチャットで `/plugin` から joshicrea-threads-ops を無効化・削除、の順で行ってください。

## Mac

1. `~/.claude/threads-ops/app/アンインストール.command` をダブルクリック（中身は `python3 uninstall.py`。自動起動の解除 → 本体停止 → プラグイン登録の削除 → フォルダ削除）
2. Claude Code を閉じて開き直す
3. Threads 側の連携は Windows の 3 と同じ

## ZIP で入れた場合

`自動起動を解除.cmd` → `停止.cmd` → 展開したフォルダを削除。プラグイン登録は無いので、それで終わりです。
